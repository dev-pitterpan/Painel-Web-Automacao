import {NextRequest,NextResponse} from "next/server";
export async function POST(req:NextRequest){const url=process.env.N8N_REPROCESS_WEBHOOK_URL;if(!url)return NextResponse.json({error:"Webhook n8n ainda não configurado."},{status:503});const body=await req.json();const payload={sku:String(body?.sku||"").trim(),titulo:String(body?.titulo||"").trim(),origem:"dashboard-pitter-pan"};if(!payload.sku)return NextResponse.json({error:"SKU obrigatório."},{status:400});const headers:Record<string,string>={"Content-Type":"application/json"};if(process.env.N8N_REPROCESS_TOKEN)headers["x-pitterpan-token"]=process.env.N8N_REPROCESS_TOKEN;const r=await fetch(url,{method:"POST",headers,body:JSON.stringify(payload),cache:"no-store"});return NextResponse.json({ok:r.ok,status:r.status,response:await r.text()},{status:r.ok?200:502})}
